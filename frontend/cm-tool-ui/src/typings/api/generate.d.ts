declare namespace Api {
  /**
   * namespace Generate
   *
   * backend api module: "generate"
   */
  namespace Generate {
    /** 雪花 ID（字符串） */
    type SnowflakeId = string;

    /** UUID（字符串） */
    type UUID = string;

    /** 随机字符串 */
    type RandomString = string;
  }
}
